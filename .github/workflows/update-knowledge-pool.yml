name: Update Knowledge Pool

on:
  push:
    branches:
      - master

jobs:
  update-knowledge-pool:
    runs-on: ubuntu-latest
    # Skip when the commit was made by the bot itself to prevent infinite loops.
    if: github.actor != 'github-actions[bot]'

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # fetch-depth: 2 ensures both the latest commit and its parent are
          # available so that `git show HEAD` can produce a meaningful diff.
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate knowledge pool entry
        run: python scripts/generate_knowledge_entry.py

      - name: Commit and push updated Knowledge_Pool.md
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/Knowledge_Pool.md
          if git diff --cached --quiet; then
            echo "No changes to docs/Knowledge_Pool.md — skipping commit."
          else
            git commit -m "docs: update Knowledge_Pool.md for commit ${{ github.sha }}"
            git push
          fi
